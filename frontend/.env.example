# API endpoint for client-side requests (used in Camera.tsx)
# Defaults to /api/analyze if not set
NEXT_PUBLIC_API_URL=/api/analyze

# Backend server URL for Next.js API rewrites (used in next.config.ts)
# Defaults to http://localhost:8000 if not set
BACKEND_URL=http://localhost:8000
